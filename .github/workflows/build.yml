on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Keep the build workflow aligned with the pinned ZMK source revision.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@0331b7d16e80954b807917f9323e59ffc1e3b626
